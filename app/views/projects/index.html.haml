%h1 Company Profile
#projects.transitions-enabled
	- @projects.each do |project|
		.box.panel.panel-default
			= link_to (image_tag project.image_url.to_s),project
			.pannel-body
				%h2= link_to project.customer , project
				%h3= project.location